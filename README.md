# Cellular Automata Simulator 🧫

Клеточный автомат на Python с возможностями различной визуализации и параметрическими стартовыми состияниями.

## ✨ Особенности

- **Интарактивная Настройка Правил и Пресэты**
- **Богатая визуализация** — 6 режимов отрисовки активных и 2 режима неактивных клеток невключая стандартные
- **Параметрические Стартовые Состояния** — Фигуры и Шумы
- **Высокая производительность (для пайтона)** — оптимизированные NumPy вычисления
- **Конфигурация** — сохранение/загрузка настроек в JSON

### Требования
- Python 3.8+
- Pyglet 2.0+
- NumPy

## Управление
- горячие клавиши
- передвижение по 3 подменю клавишами с подсказками в каждой 'ветке древа'
- возможно самая высокая производительность (не считая шейдеры) пробовал разбивать матрицу на сигменты и обрабатывать multiprocessing'ом -> медление даже для матриц 1920x1200;  f2py тоже будет медленее (скорее всего, или у меня не получилось запустить или производительность как описана выше), возможно получится получить прирост если прощитывать 5+ кадров на перёд (с shared memory для multiproc), тогда процент наклодных расходов уменьшиться (не точно) пробовал несколько методов обработки матрицы numpy'ем (и scipy);   остальные методы(numba, pyPy) оптимизации помогут не сильно так как основное время уходит именно на обработку матрицы   (30ms total;  ~1.3ms render;  ~1.0 ui;  ~26.0 прощёт кадра numpy);   вот алгоритм:
  ```python
  neighbors = (
    np.roll(np.roll(grid, 1, 0), 1, 1) +
    np.roll(grid, 1, 0) +
    np.roll(np.roll(grid, 1, 0), -1, 1) +
    ...#5 строк пропущено
  ) #всего 8 строк

  grid = np.where((grid == 0) & (rule_b[neighbors]) | (grid == 1) & (rule_s[neighbors]), 1, 0).astype(np.uint8)
  ```
  не много упращённая версия;  теперь эта функция сложнее, из-за разных режимов отрисовки;  но принцип такой-же
- рендер такой: эта матрица преобразуется в текстуру и выводится на экран

## Написание и использование библиотек
- нейросети:    DeepSeek: дольшую часть;    я: 'напровление' нейросити, общая структура кода, базовое испровление ошибок, некоторые (восновном интирестные) алгоритмы, copy+paste
- Pyglet, NumPy, colorsys(для генерации палитры цветов)

## Ближайщее развитие
- добавление второго контекстного окна через зажатый shift (как несколько терминалов) для удобного управления (система древовидная)
- чтение файлов rle
- 'отвязать' понятия разрешение экрана и сетка (сейчай есть cell_size !даже float`ы и меньше 1) но хотелось бы свободное передвижение 'камеры'
- параметрические окресности
- сохронение изображений

## цель программы
- быть хорошим soft`ом
- предостовлять хороший опыт
- вдохновлять пользователей на творчество, тагже как меня @OnigiriScience(www.youtube.com/@OnigiriScience) и @foo52ru(www.youtube.com/@foo52ru)
